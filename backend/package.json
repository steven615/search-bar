{
  "name": "search-bar-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "json-server --watch data.json --routes routes.json --port 3001"
  }
}
